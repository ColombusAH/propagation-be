web: sh -c "export BACKEND_HOST=$(echo $BACKEND_URL | sed 's|https\?://||' | sed 's|/.*||') && envsubst '\$PORT \$BACKEND_URL \$BACKEND_HOST' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
