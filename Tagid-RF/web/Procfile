web: sh -c "BACKEND_HOST=$(echo $BACKEND_URL | awk -F/ '{print $3}') && export BACKEND_HOST && envsubst '\$PORT \$BACKEND_URL \$BACKEND_HOST' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && cat /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
